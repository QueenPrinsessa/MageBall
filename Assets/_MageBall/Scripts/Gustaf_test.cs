using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gustaf_test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //test ladidadida
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
